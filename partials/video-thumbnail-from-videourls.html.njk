
{% import "ak_embeddables_macros.njk" as ak_embeddables with context %}

{% if videoUrls %}
    {{ ak_embeddables.doVideoThumbnailsFromVideoURLS(videoUrls) }}
{% endif %}

{#
<%
  if (typeof videoUrls !== "undefined" && videoUrls && videoUrls[0] && videoUrls[0].url) {
    %><embed-resource
            template="embed-thumbnail.html.ejs"
            style="max-width: 100%;"
            width="200"
            align="right"
            href="<%= videoUrls[0].url %>"/><%
  } %>
#}
