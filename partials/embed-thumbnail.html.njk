<img 
    {% if embedClass %}class="{{ embedClass }}" {% endif %}
    {% if width %} width="{{ width }}" {% endif %}
    {% if align %} align="{{ align }}" {% endif %}
    {% if style %} style="{{ style }}" {% endif %}
    {% if alt %} alt="{{ alt }}" {% endif %}
    {% if title %} title="{{ title }}" {% endif %}
    src="{{ imageUrl }}" />

<opengraph-image href="{{ imageUrl }}"/>
