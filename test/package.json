{
  "name": "akashacms-embeddables-test",
  "version": "1.0.0",
  "description": "Test suite for @akashacms/plugins-embeddables module",
  "main": "index.mjs",
  "scripts": {
    "test": "mocha ./index.mjs",
    "setup": "npm install && cd .. && npm install && npm install ../akasharender --no-save",
    "clean": "rm -rf node_modules out"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^12.1.x",
    "chai": "^5.1.x",
    "mocha": "^10.8.x",
    "@akashacms/plugins-base": "akashacms/akashacms-base#0.9"
  }
}
